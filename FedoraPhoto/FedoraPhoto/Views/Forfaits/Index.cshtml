@model IEnumerable<FedoraPhoto.Models.Forfait>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomForfait)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionForfait)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PrixForfait)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NbPhotos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Temps)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomForfait)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionForfait)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PrixForfait)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NbPhotos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Temps)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ForfaitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ForfaitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ForfaitID })
        </td>
    </tr>
}

</table>
